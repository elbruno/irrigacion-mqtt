# =================================================================
# VARIABLES DE ENTORNO - EJEMPLO PARA PRODUCCIÓN
# =================================================================
# 
# Este archivo muestra las variables necesarias para ejecutar el proyecto.
# 
# USO:
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env con tus valores reales
# 3. NUNCA subas el archivo .env al repositorio
#
# =================================================================

# PostgreSQL
POSTGRES_DB=irrigacion
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_contraseña_segura_aqui
POSTGRES_PORT=5432

# Spring Boot / Backend
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/irrigacion?options=-c%20TimeZone=UTC
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=tu_contraseña_segura_aqui

# MQTT Configuration
APP_MQTT_ENABLED=true
APP_MQTT_HOST=localhost
APP_MQTT_PORT=1883
APP_MQTT_USERNAME=tu_usuario_mqtt
APP_MQTT_PASSWORD=tu_contraseña_mqtt
APP_MQTT_TLS=true
APP_MQTT_CLIENT_ID=irrigacion-backend-prod

# Cloudflare (opcional)
CLOUDFLARE_TUNNEL_TOKEN=tu_token_de_cloudflare_aqui

# Backend Port
SERVER_PORT=8080

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database Connection Pool
DB_POOL_SIZE=20
DB_POOL_MIN=5

# Timezone
TZ=UTC

# =================================================================
# NOTAS DE SEGURIDAD
# =================================================================
# 
# ⚠️  En PRODUCCIÓN:
# - Usa contraseñas fuertes (mínimo 16 caracteres)
# - Habilita TLS en MQTT (puerto 8883)
# - Usa certificados válidos
# - No uses valores por defecto como "postgres"
# - Considera usar gestores de secretos (AWS Secrets Manager, etc.)
# - Configura CORS_ALLOWED_ORIGINS con tu dominio real
# - Habilita autenticación y autorización (próxima iteración)
# - Monitorea métricas en /actuator/metrics
#
# =================================================================
